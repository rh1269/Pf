<nav class="navbar navbar-default navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <!--  collapsed button -->
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!-- logo -->
      <%= link_to "PillowFort", root_path, class: "navbar-brand"%>
    </div>

    <div class="navbar-title hidden-xs">
      <%= (@page_title || "Your Feed") if user_signed_in? %>
    </div>

    <div class="collapse navbar-collapse" id="collapsible-navbar-menu">
      <ul class="nav navbar-right">
        <% if user_signed_in? %>
          <li class="navbar-text hidden-xs">
            <p>
              Logged in as
              <span class="nav-highlight">
                <%= link_to current_user.username, user_path(current_user.username) %>
              </span>
            </p>
          </li>
          <li class="nav-logout">
            <%= link_to signout_path, class: 'signout' do %>
              <span class="glyphicon glyphicon-off"></span>
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to "Login", new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
